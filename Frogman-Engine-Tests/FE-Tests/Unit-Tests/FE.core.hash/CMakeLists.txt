SET(TEST_TARGET "FE.core.hash_test")

SOURCE_GROUP("${TEST_TARGET}" FILES ${FE_CORE_SOURCES})

ADD_EXECUTABLE(${TEST_TARGET}
${FE_CORE_SOURCES}
${THIRD_PARTY_CITY_HASH}
main.cpp
)

SET_TARGET_PROPERTIES(${TEST_TARGET} PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})

TARGET_LINK_LIBRARIES(${TEST_TARGET} 
${BOOST_STACKTRACE} 
${BOOST_CHRONO} 
${UNIT_TEST} 
${BENCHMARK_TEST}
)