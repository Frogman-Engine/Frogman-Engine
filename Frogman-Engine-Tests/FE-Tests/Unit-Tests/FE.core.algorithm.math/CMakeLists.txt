SET(TEST_TARGET "FE.core.algorithm.math_test")

SOURCE_GROUP("${TEST_TARGET}" FILES ${FE_CORE_ALGORITHM_MATH})


ADD_EXECUTABLE(${TEST_TARGET}
main.cpp
${FE_CORE_PREREQUISITES}
${FE_CORE_LOG}
${FE_CORE_ALGORITHM_MATH}
)

SET_TARGET_PROPERTIES(${TEST_TARGET} PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})

TARGET_LINK_LIBRARIES(${TEST_TARGET} 
${BOOST_STACKTRACE} 
${BOOST_CHRONO} 
${UNIT_TEST} 
${BENCHMARK_TEST}
)