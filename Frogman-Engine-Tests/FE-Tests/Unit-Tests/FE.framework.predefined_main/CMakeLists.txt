SET(TEST_TARGET "FE.framework.predefined_main_test")

SOURCE_GROUP("${TEST_TARGET}" FILES ${FE_CORE} ${FE_FRAMEWORK})

ADD_EXECUTABLE(${TEST_TARGET}
${FE_CORE}
${FE_FRAMEWORK}
${THIRD_PARTY_CITY_HASH}
main.cpp
)

SET_TARGET_PROPERTIES(${TEST_TARGET} PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})

TARGET_LINK_LIBRARIES(${TEST_TARGET} 
${BOOST_STACKTRACE} 
${BOOST_CHRONO} 
${UNIT_TEST} 
${BENCHMARK_TEST}
)